name: Scrape WPL Fantasy Points

on:
  # Manual trigger from GitHub Actions tab
  workflow_dispatch:

  # Scheduled: Run daily at 7 PM and 8 PM UTC
  schedule:
    - cron: '0 19 * * *'
    - cron: '0 20 * * *'

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper
        run: |
          python scripts/scrape_fantasy.py

      - name: Commit and push if changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add instance/auction.db
          git diff --staged --quiet || git commit -m "Auto-update fantasy points $(date +'%Y-%m-%d %H:%M UTC')"
          git push
